VITE_SUPABASE_URL="https://YOUR-PROJECT-ID.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_PUBLIC_ANON_KEY"

# Copy this file to `.env.local` (for local dev) or `.env`
# and replace the values with your Supabase project's URL and anon key.

